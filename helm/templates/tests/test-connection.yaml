apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "dg-edge-api-caller.fullname" . }}-test-connection"
  labels:
    {{- include "dg-edge-api-caller.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "dg-edge-api-caller.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
